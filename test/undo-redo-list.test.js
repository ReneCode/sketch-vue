/*
let should = require('chai').should();

import undoRedoList from '../src/store/modules/undo-redo-list'

describe('UndoRedoList', () => {
  it('should start with canUndo/Redo false', () => {
    undoRedoList.canRedo.should.be.false;
    undoRedoList.canUndo.should.be.false;
  });

});
*/
